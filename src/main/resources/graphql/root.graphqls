type Query {
    users(page: PageRequest): UserPage
    products(page: PageRequest): ProductPage
    categories(page: PageRequest): CategoryPage
}

#type Mutation {
#    signInUser(email: String, password: String): SigninPayload
#}

input PageRequest {
    page: Int
    size: Int
    sorts: [String]
}

interface Page {
    totalElements: Int
    numberOfElements: Int
    totalPages: Int!
    number: Int!
    size: Int!
}