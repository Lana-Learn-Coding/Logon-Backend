type Category {
    id: ID!
    createdAt: String!
    modifiedAt: String!

    name: String
    products: [Product]
}

type CategoryPage implements Page {
    content: [Category]
    totalElements: Int
    totalPages: Int
    number: Int
    numberOfElements: Int
    size: Int
}